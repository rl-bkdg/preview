import index from "./lib/jquery/index.js";

(function (factory) {
    if (typeof define === "function" && define.amd) {
        // AMD
        define(["jquery"], factory);
    } else if (typeof exports === "object") {
        // CommonJS
        factory(require("jquery"));
    } else {
        // Browser globals
        factory(jQuery);
    }
})(function ($) {
    index($);

    // Access the current page slug from the localized data.
    if (
        //'shops' === entryData?.currentPageSlug
        ["shops", "shops-0"].includes(entryData?.currentPageSlug)
    ) {
        // Use a dynamic import() to load shops.es.js
        import("./page/shops/dist/index.es.js")
            .then((module) => {
                module.default($);
            })
            .catch((error) => {
                console.error("Failed to load shops.es.js", error);
            });
    }
});
